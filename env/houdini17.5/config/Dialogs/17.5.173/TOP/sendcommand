// Magic: 61a577fc
// Automatically generated PSI dialog (PSI version 17.5.173)
// Mon Sep  2 02:06:03 2019


f_tmp = FLOAT_FIELD;	// dialog.top.ui

pdg_workitemgeneration.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH

    PARM_LABEL_SIMPLE("Work Item Generation", "pdg_workitemgeneration") WIDTH(1.5);
    { WIDTH(0.09) }

    pdg_workitemgeneration.menu;

    { HSTRETCH }

}
setsharedservername.row.join =		
{
  LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)
setsharedservername.join =		
{
  LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)

setsharedservername.gad = PARM_BUTTON("setsharedservername", "") toggle VALUE(setsharedservername.val);

sharedservername.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    sharedservername.label = PARM_LABEL("Shared Server Name", "sharedservername") WIDTH(1.32);
    PARM_FIELD(string, "sharedservername") VALUE(sharedservername.val) 
	LABELREF(sharedservername.label) HSTRETCH;

}
}	
}	// Template join.bot


resultdatatag.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    resultdatatag.label = PARM_LABEL("Result Data Tag", "resultdatatag") WIDTH(1.5);
    PARM_FIELD(string, "resultdatatag") VALUE(resultdatatag.val) 
	LABELREF(resultdatatag.label) HSTRETCH;

}

commandstring.gad = ROW
{

    HSTRETCH JUSTIFY(left, top)

    commandstring.label = PARM_LABEL("Command Script", "commandstring");

    MULTILINE_PARM_FIELD ("commandstring") 

    	LABELREF(commandstring.label) 

    	VALUE(commandstring.val) 
    	SYNTAX("python") 
    	LINES(8,40) 
    	HSTRETCH;

    {

	LAYOUT(vertical)
	{
	    SIZE(0, 0.26)
	}

    }

}

folder0_1.gad = 
{
    LAYOUT(vertical) HSTRETCH

    TABDIALOG2 "folder0_1.gad"
    { 
	VALUE(folder0_1.gad.val) STRETCH(1,0)

	TAB "Node"
	{
	    LAYOUT(vertical) STRETCH

	    MARGIN(0.05) SPACING(2p)
	    pdg_workitemgeneration.gad;		
	    setsharedservername.row.join;		
	    resultdatatag.gad;		
	    commandstring.gad;		

	    { STRETCH(1,1) LOOK(plain) }
	}
    } 
}

dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH LOOK(plain) 
    folder0_1.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
